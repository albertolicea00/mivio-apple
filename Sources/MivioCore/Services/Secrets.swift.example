// Copy this file to `Secrets.swift` in this same folder (it's gitignored — never commit real keys).
// Get a free TMDB API key at: https://www.themoviedb.org/settings/api

enum Secrets {
    static let tmdbDefaultAPIKey = ""
}
